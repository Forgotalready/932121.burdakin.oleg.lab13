@model QuestionViewModel

@{
    ViewData["Title"] = "Quiz";
    Layout = "_Layout";
}

@Model.ShowQuestion()

@using (Html.BeginForm(null, null, method: FormMethod.Post))
{
    @Html.HiddenFor(m => m.Question.FirstNumber)
    @Html.HiddenFor(m => m.Question.SecondNumber)
    @Html.HiddenFor(m => m.Question.Operation)
    @Html.TextBoxFor(m => m.Answer)

    <button type="submit" formaction="@Url.Action("Index", "Quiz")">Next</button>
    <button type="submit" formaction="@Url.Action("Result", "Quiz")">Finish</button>
}
